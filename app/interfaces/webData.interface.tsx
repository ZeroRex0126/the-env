export interface featureWorkDat {
  heading: string;
  imgSrc: string;
  url: string;
}
export interface faqDat {
  heading: string;
  detail: string;
}

export interface serviceDat {
  heading: string;
  imgSrc: string;
  paragraph: string;
}